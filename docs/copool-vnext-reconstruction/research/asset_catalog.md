# 资产目录与 clean-room 处理

- P1 的现有图标、截图、SwiftUI 组件和本地化资源属于目标项目资产，可继续维护。
- P2 README 中的 Dashboard、Voice、Session、Catalog 和 Agent 截图只作为功能证据，不进入实现或设计稿。
- P3 的 tray/Tauri UI、图标和 CSS 不进入实现。
- 新供应商图标默认使用 SF Symbol、文字首字母或用户自行提供的合规资产；不从参考仓库复制品牌图。
- 所有新增文案采用 Copool 自身语气和本地化 key。
