# Phase 05 — Catalog、Routes、Usage 与 UI

目标：完成凭据感知目录、策展、RoutePolicy、DecisionTrace 和 Models UI。

- provider/live/user/registry/fallback 元数据优先级。
- credential missing 不进入默认可用目录。
- explicit/alias/auto 三类选择。
- hard constraints + score + failover，保存 trace。
- 使用量来源明确区分。
- 顶层 5 Tab 不变，Models 二级导航。

验收：AC-011/012/013/015；固定宽度 UI snapshot 通过。
